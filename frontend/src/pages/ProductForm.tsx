import{useEffect,useState,type FormEvent}from'react';import{useNavigate,useParams}from'react-router-dom';import{api}from'../api/client';import{Header,Notice}from'../components/Layout';import type{Product}from'../types';
const blank={sku:'',name:'',description:'',price:'0.00',stock_quantity:0,is_active:true};export default function ProductForm(){const{id}=useParams(),navigate=useNavigate();const[form,setForm]=useState(blank),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{if(id)api<Product>(`/products/${id}`).then(p=>setForm({sku:p.sku,name:p.name,description:p.description,price:p.price,stock_quantity:p.stock_quantity,is_active:p.is_active})).catch(e=>setError(e.message))},[id]);const set=(k:string,v:unknown)=>setForm(f=>({...f,[k]:v}));async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await api(id?`/products/${id}`:'/products',{method:id?'PUT':'POST',body:JSON.stringify(form)});navigate('/products',{state:{message:`Product ${id?'updated':'created'}`}})}catch(e){setError(e instanceof Error?e.message:'Save failed')}finally{setBusy(false)}}return <><Header title={id?'Edit product':'New product'}/><form className="panel form" onSubmit={submit}><Notice text={error}/><label>SKU<input value={form.sku} onChange={e=>set('sku',e.target.value)} required/></label><label>Product name<input value={form.name} onChange={e=>set('name',e.target.value)} required/></label><label>Description<textarea value={form.description} onChange={e=>set('description',e.target.value)}/></label><div className="grid2"><label>Price<input type="number" min="0" step="0.01" value={form.price} onChange={e=>set('price',e.target.value)} required/></label><label>Stock quantity<input type="number" min="0" value={form.stock_quantity} onChange={e=>set('stock_quantity',Number(e.target.value))} required/></label></div><label className="check"><input type="checkbox" checked={form.is_active} onChange={e=>set('is_active',e.target.checked)}/> Active</label><div className="actions"><button type="button" className="secondary" onClick={()=>navigate('/products')}>Cancel</button><button disabled={busy}>{busy?'Saving…':'Save product'}</button></div></form></>}
