import{useState,type FormEvent}from'react';import{Link,Navigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{Notice}from'../components/Layout';
export default function Login(){const{user,login}=useAuth();const[email,setEmail]=useState('admin@orderflow.local'),[password,setPassword]=useState('Admin123!'),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to="/"/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password)}catch(e){setError(e instanceof Error?e.message:'Login failed')}finally{setBusy(false)}}return <div className="login"><section><div className="brand"><span>OF</span>OrderFlow</div><h1>Welcome back</h1><p>Manage orders, customers and inventory from one clear workspace.</p><form onSubmit={submit}><Notice text={error}/><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label><div className="password-help"><Link to="/reset-password">Forgot password?</Link></div><button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p className="auth-link">Need an account? <Link to="/register">Create one</Link></p></form></section></div>}
