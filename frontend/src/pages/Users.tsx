import{useEffect,useState,type FormEvent}from'react';import{api,qs}from'../api/client';import{Header,Notice}from'../components/Layout';import type{Page,Role,User}from'../types';
export default function Users(){const[data,setData]=useState<Page<User>>(),[error,setError]=useState(''),[show,setShow]=useState(false),[form,setForm]=useState({email:'',full_name:'',password:'',role:'staff' as Role});const load=()=>api<Page<User>>('/users'+qs({page_size:100})).then(setData).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function submit(e:FormEvent){e.preventDefault();try{await api('/users',{method:'POST',body:JSON.stringify(form)});setShow(false);setForm({email:'',full_name:'',password:'',role:'staff'});load()}catch(e){setError(e instanceof Error?e.message:'Save failed')}}async function toggle(u:User){if(!confirm(`${u.is_active?'Deactivate':'Activate'} ${u.full_name}?`))return;try{await api(`/users/${u.id}/status`,{method:'PATCH',body:JSON.stringify({is_active:!u.is_active})});load()}catch(e){setError(e instanceof Error?e.message:'Update failed')}}return <><Header title="User management"><button onClick={()=>setShow(v=>!v)}>Add user</button></Header><Notice text={error}/>{show&&<form className="panel form" onSubmit={submit}><h2>New user</h2><label>Full name<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value as Role})}><option value="staff">Staff</option><option value="admin">Admin</option></select></label><button>Create user</button></form>}<div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Action</th></tr></thead><tbody>{data?.items.map(u=><tr key={u.id}><td>{u.full_name}</td><td>{u.email}</td><td>{u.role}</td><td><span className={`badge ${u.is_active?'active':'inactive'}`}>{u.is_active?'Active':'Inactive'}</span></td><td><button className="link" onClick={()=>toggle(u)}>{u.is_active?'Deactivate':'Activate'}</button></td></tr>)}</tbody></table></div></>}
